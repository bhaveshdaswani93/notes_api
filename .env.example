# Environment example for Notes API
# Copy this to `.env` and fill values before running the app.

# Auth0 configuration (required for production/demo using Auth0)
# - `AUTH0_DOMAIN`: your Auth0 tenant domain (e.g. your-tenant.auth0.com)
# - `AUTH0_AUDIENCE`: the API identifier/audience configured in Auth0
# - `AUTH0_ISSUER`: optional, defaults to https://<AUTH0_DOMAIN>/ if not provided
AUTH0_DOMAIN=https://dev-cv0sheaqu1u2rrv4.us.auth0.com
AUTH0_AUDIENCE=urn:notes-api
#AUTH0_ISSUER=https://your-tenant.auth0.com/

# Optional: local JWT secret (used only if local signing is enabled)
# If you rely solely on Auth0-issued tokens you can ignore this.
JWT_SECRET=10f2a7b3-385c-4552-9995-f9ab2f5b9ff9

# Optional OAuth2 provider settings (used by `OAuthStrategy` if configured)
OAUTH_AUTHORIZE_URL=
OAUTH_TOKEN_URL=
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_CALLBACK_URL=http://localhost:3000/auth/mcp/callback
OAUTH_USERINFO_URL=

# App
PORT=3000
